import { DEFAULT_COMPOSITION } from "./defaultComposition.js";
// Stub pour build — en prod ça ira via Supabase
export async function seedDefaultComposition(weddingId, moments) {
  console.log("seed stub", weddingId);
  return [];
}
